{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["manager"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:allow-read-dir", {
      "identifier": "fs:scope",
      "allow": [{ "path": "$RESOURCE" }]
    }
  ]
}
